# imgpick 项目学习笔记

## 项目概述

imgpick 是一个使用 Slint + Rust 开发的 Windows 桌面图片标价应用，支持图片管理、标价、Excel 配对等功能。通过这个项目的开发，我学到了很多关于 Slint UI 框架、Rust 异步编程、文件处理等方面的知识。

---

## 一、Slint UI 框架学习

### 1.1 基础语法

Slint 是一个声明式的 UI 框架，语法类似于 JSON 和 CSS 的结合体。

**组件定义**：
```slint
export component MyComponent inherits Rectangle {
    in-out property <string> text: "";
    in-out property <int> count: 0;
    
    callback clicked();
    
    width: 100px;
    height: 30px;
    background: #1976D2;
    
    Text {
        text: root.text;
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
```

**关键点**：
- `export` 关键字使组件可以被其他文件引用
- `in-out property` 定义可读写的属性
- `callback` 定义回调函数
- `root` 引用当前组件

### 1.2 全局单例

全局单例用于在 Rust 和 Slint 之间传递数据：

```slint
export global MyAdapter {
    in-out property <string> data: "";
    in-out property <int> index: -1;
    
    callback do-something();
}
```

在 Rust 中访问：
```rust
app.global::<ui::MyAdapter>().set_data("value".into());
let data = app.global::<ui::MyAdapter>().get_data();
```

**关键经验**：
- 全局单例定义在 `app.slint` 中
- Rust 通过 `app.global::<ui::GlobalName>()` 访问
- 属性名在 Rust 中会自动转换为 snake_case

### 1.3 条件渲染

```slint
if condition: Rectangle {
    // 当 condition 为 true 时显示
}

if !condition: Rectangle {
    // 当 condition 为 false 时显示
}
```

### 1.4 循环渲染

```slint
for item[i] in model: Rectangle {
    Text {
        text: item.name;
    }
}
```

**关键经验**：
- `i` 是索引，可以用于高亮选中项
- 模型变化时会自动重新渲染
- 空模型需要用 `if` 条件守卫，否则会崩溃

### 1.5 样式和布局

**Flex 布局**：
```slint
HorizontalLayout {
    spacing: 8px;
    padding: 12px;
    
    Rectangle { horizontal-stretch: 1; }
    Rectangle { width: 100px; }
}
```

**关键经验**：
- `horizontal-stretch: 1` 表示自动扩展
- `width: 100px` 表示固定宽度
- `spacing` 控制子元素间距

**条件样式**：
```slint
background: is-selected ? #e3f2fd : #fafafa;
color: is-hover ? #1976D2 : #333;
```

---

## 二、组件拆分尝试

### 2.1 问题描述

`app.slint` 文件达到 2379 行，尝试拆分为多个文件。

### 2.2 失败原因

1. **全局单例引用问题**
   - 全局单例定义在 `app.slint` 中
   - 拆分到其他文件后，`main.rs` 中的 `app.global::<ui::PlanPageAdapter>()` 无法找到类型
   - Slint 编译器生成的 Rust 代码会将所有类型放在 `ui` 模块中

2. **导入路径限制**
   - 相对路径导入（如 `import { PlanData } from "../structs.slint"`）无法正确解析
   - `std-widgets.slint` 中的组件不是通过 `import` 导入的

3. **main.rs 类型引用依赖**
   - 类型必须在 Slint 编译时被正确生成到 `ui` 模块中

### 2.3 结论

由于 Slint 的模块系统限制，`app.slint` 必须保持单文件结构。

**关键经验**：
- Slint 的模块系统还不够成熟
- 全局单例必须定义在主文件中
- 组件拆分会导致类型引用失败

---

## 三、Excel 导入功能

### 3.1 Excel 文件解析

使用 `calamine` 库读取 Excel 文件：

```rust
use calamine::{Reader, open_workbook, Xlsx, Data as CellData};

fn read_excel_file(path: &Path) -> Result<Vec<ExcelRow>, String> {
    let mut workbook: Xlsx<_> = open_workbook(path)
        .map_err(|e| format!("Failed to open Excel file: {}", e))?;
    
    let sheet_names = workbook.sheet_names().to_vec();
    let sheet_name = &sheet_names[0];
    let range = workbook.worksheet_range(sheet_name)
        .map_err(|e| format!("Failed to read sheet: {}", e))?;
    
    // 处理数据...
}
```

### 3.2 列名自动检测

支持多种列名格式，自动检测列名行：

```rust
let keywords = ["sampleid", "sample_id", "样本编号", "samplebarcode", "样本id", 
                 "孔位1", "孔位2", "孔位3", "考察时间", "检测时间"];

// 尝试前3行作为列名备选
for check_row in 0..3 {
    let mut found_keywords = 0;
    // 检查是否包含关键词
    for keyword in &keywords {
        if header_lower.contains(keyword) {
            found_keywords += 1;
        }
    }
    // 如果找到至少2个关键词，认为这是列名行
    if found_keywords >= 2 {
        header_row_index = check_row;
        break;
    }
}
```

**关键经验**：
- 不同 Excel 文件的列名格式可能不同
- 需要支持多种列名格式
- 自动检测列名行可以提高用户体验

### 3.3 孔位结果合并

孔位结果分散在多个列中，需要合并：

```rust
// 查找所有孔位列
let mut hole_columns: Vec<(usize, i32)> = Vec::new();
for (j, header) in headers.iter().enumerate() {
    for hole_num in 1..=8 {
        let patterns = vec![
            format!("孔位{}", hole_num),
            format!("hole{}", hole_num),
        ];
        for pattern in &patterns {
            if header.to_lowercase().contains(pattern) {
                hole_columns.push((j, hole_num));
            }
        }
    }
}

// 按孔位编号排序后合并
hole_columns.sort_by_key(|&(_, num)| num);
let hole_result: Vec<String> = hole_columns.iter()
    .map(|&(idx, _)| row[idx].clone())
    .collect();
let hole_result_str = hole_result.join(",");
```

### 3.4 Excel 日期时间转换

Excel 的日期时间是序列号格式，需要转换：

```rust
CellData::DateTime(dt) => {
    let value = dt.as_f64();
    let excel_epoch = chrono::NaiveDate::from_ymd_opt(1899, 12, 30).unwrap();
    let days = value as i64;
    let fractional = value - days as f64;
    let seconds = (fractional * 86400.0) as u32;
    
    if let Some(date) = excel_epoch.checked_add_days(chrono::Days::new(days as u64)) {
        let time = chrono::NaiveTime::from_num_seconds_from_midnight_opt(seconds, 0).unwrap();
        let datetime = date.and_time(time);
        datetime.format("%Y-%m-%d %H:%M:%S").to_string()
    }
}
```

**关键经验**：
- Excel 日期基准是 1899 年 12 月 30 日
- `as_f64()` 方法获取数值
- 小数部分表示时间（0.5 = 12:00:00）

---

## 四、图片匹配功能

### 4.1 时间格式匹配

根据考察时间查找对应的图片：

```rust
// 时间格式：2026-04-09 12:25:49
// 图片名格式：2026-04-09-12-25-49-679.jpg

fn parse_time_from_filename(filename: &str) -> Option<String> {
    // 移除扩展名（使用 split 而不是 rsplit）
    let name = filename.split('.').next().unwrap_or(filename);
    
    let parts: Vec<&str> = name.split('-').collect();
    if parts.len() >= 6 {
        let date = format!("{}-{}-{}", parts[0], parts[1], parts[2]);
        let time = format!("{}:{}:{}", parts[3], parts[4], parts[5]);
        Some(format!("{} {}", date, time))
    } else {
        None
    }
}
```

**关键经验**：
- `split('.').next()` 获取文件名主体
- `rsplit('.').next()` 获取扩展名（方向相反）
- 移除扩展名应该用 `split` 而不是 `rsplit`

### 4.2 价位完全匹配

```rust
fn match_by_price(hole_result: &str, images: &[Image], category: &str) -> Vec<ImageCandidate> {
    let target_slots: Vec<&str> = hole_result.split(',').collect();
    let mut candidates = Vec::new();
    
    for image in images {
        if let Some(ref price) = image.price {
            let image_slots: Vec<&str> = price.split(',').collect();
            
            // 完全匹配检查
            if target_slots == image_slots {
                candidates.push(ImageCandidate {
                    file_name: image.file_name.clone(),
                    match_score: 100,
                    // ...
                });
            }
        }
    }
    
    candidates
}
```

### 4.3 最终目录文件复制

```rust
// 复制并重命名文件
let src_path = src_dir.join(&image_name);
let dest_path = final_dir.join(&new_name);

if src_path.exists() {
    let _ = std::fs::copy(&src_path, &dest_path);
}
```

**关键经验**：
- 使用 `let _ =` 忽略文件不存在的错误
- 删除文件和数据库记录都不会报异常

---

## 五、独立窗口组件

### 5.1 创建独立窗口

```slint
export component MyWindow inherits Window {
    title: "My Window";
    background: white;
    width: 600px;
    height: 500px;
    
    in-out property <string> data: "";
    
    callback close-window();
    callback confirm();
    
    VerticalLayout {
        // 内容...
    }
}
```

### 5.2 在 Rust 中创建并显示

```rust
let window = ui::MyWindow::new().unwrap();

// 设置属性
window.set_data("value".into());

// 设置回调
let weak = window.as_weak();
window.on_close_window(move || {
    if let Some(w) = weak.upgrade() {
        let _ = w.window().hide();
    }
});

// 显示窗口
window.show().unwrap();
```

**关键经验**：
- 窗口关闭需要调用 `window().hide()`
- 回调中需要使用 `weak` 引用避免循环引用
- 属性需要在显示前设置

### 5.3 窗口间数据传递

使用全局适配器作为"剪贴板"：

```rust
// 窗口A：写入数据
app.global::<ui::MyAdapter>().set_clipboard_data("value".into());

// 窗口B：读取数据
let data = app.global::<ui::MyAdapter>().get_clipboard_data();
```

---

## 六、Card 切换功能

### 6.1 Card 数据结构

```slint
export global ExcelPageAdapter {
    // 当前活动的card: 0=血型, 1=抗筛, 2=交叉配血
    in-out property <int> active-card: 0;
    
    // 三个card的Excel数据
    in-out property <[ExcelRowData]> excel-rows-abo: [];
    in-out property <[ExcelRowData]> excel-rows-as: [];
    in-out property <[ExcelRowData]> excel-rows-cm: [];
    
    // 当前显示的Excel数据
    in-out property <[ExcelRowData]> current-excel-rows: [];
    
    callback switch-card(int);
    
    switch-card(index) => {
        active-card = index;
        if (index == 0) {
            current-excel-rows = excel-rows-abo;
        } else if (index == 1) {
            current-excel-rows = excel-rows-as;
        } else if (index == 2) {
            current-excel-rows = excel-rows-cm;
        }
        selected-row = -1;
        status-message = "";
    }
}
```

### 6.2 Card 切换 UI

```slint
HorizontalLayout {
    height: 40px;
    spacing: 4px;
    
    Rectangle {
        horizontal-stretch: 1;
        background: ExcelPageAdapter.active-card == 0 ? #1976D2 : #f5f5f5;
        border-radius: 4px;
        
        Text {
            text: "血型";
            color: ExcelPageAdapter.active-card == 0 ? white : #666;
        }
        
        TouchArea {
            clicked => { ExcelPageAdapter.switch-card(0); }
        }
    }
    // 其他Card...
}
```

**关键经验**：
- 每个 Card 独立存储数据
- 切换 Card 时更新 `current-excel-rows`
- 切换时清除选中状态和状态消息

---

## 七、异步导入方案

### 7.1 扫描文件夹

```rust
fn scan_final_folder(final_dir: &Path) -> HashMap<String, PathBuf> {
    let mut result = HashMap::new();
    
    if !final_dir.exists() {
        return result;
    }
    
    if let Ok(entries) = std::fs::read_dir(final_dir) {
        for entry in entries.flatten() {
            if let Some(name) = entry.file_name().to_str() {
                if name.ends_with(".jpg") || name.ends_with(".png") {
                    result.insert(name.to_string(), entry.path());
                }
            }
        }
    }
    
    result
}
```

### 7.2 匹配状态恢复

```rust
// 构建时间到文件名的映射
let mut time_to_file: HashMap<String, String> = HashMap::new();
for (filename, _) in &final_images {
    if let Some(time_str) = parse_time_from_filename(filename) {
        time_to_file.insert(time_str, filename.clone());
    }
}

// 恢复匹配状态
for row in excel_rows.iter_mut() {
    let test_time = row.test_time.to_string();
    if let Some(matched_filename) = time_to_file.get(&test_time) {
        row.matched_image = matched_filename.clone().into();
    }
}
```

**关键经验**：
- 导入时自动扫描 final 文件夹
- 根据文件名时间反向匹配 Excel 行
- 避免用户重复匹配

---

## 八、DataGridView 实现

### 8.1 表头和数据行分离

```slint
// 表头
Rectangle {
    height: 36px;
    background: #1976D2;
    
    HorizontalLayout {
        Text { text: "列1"; width: 100px; }
        Text { text: "列2"; width: 150px; }
    }
}

// 数据行
ScrollView {
    VerticalLayout {
        for row[i] in data: Rectangle {
            height: 34px;
            background: i == selected-row ? #e3f2fd : white;
            
            HorizontalLayout {
                Text { text: row.col1; width: 100px; }
                Text { text: row.col2; width: 150px; }
            }
        }
    }
}
```

### 8.2 右键菜单

```slint
TouchArea {
    clicked => { root.clicked(); }
    pointer-event(event) => {
        if (event.kind == PointerEventKind.down && event.button == PointerEventButton.right) {
            root.right-clicked();
        }
    }
}
```

**关键经验**：
- 表头和数据行的列宽必须一致
- 右键使用 `pointer-event` 而不是 `clicked`
- `PointerEventButton.right` 表示右键

---

## 九、子分类展开功能

### 9.1 数据结构

```slint
export struct ImageCategoryData {
    name: string,
    display-name: string,
    count: int,
    expanded: bool,
    images: [string],
    
    // 子分类属性
    has-subcategories: bool,
    subcategory-names: [string],
    subcategory-counts: [int],
    subcategory-expanded: [bool],
    subcategory-1-images: [string],
    subcategory-2-images: [string],
    subcategory-3-images: [string],
}
```

### 9.2 独立选中状态

```slint
// 每个子分类单独跟踪选中状态
in-out property <int> selected-subcategory: -1;
in-out property <int> selected-subcategory-image: -1;

// 高亮判断
background: (selected-subcategory == 0 && selected-subcategory-image == j) ? #bbdefb : transparent;
```

**关键经验**：
- 子分类需要独立的选中状态
- 高亮判断需要同时检查子分类索引和图片索引
- 点击普通分类时清除子分类选中状态

---

## 十、状态消息管理

### 10.1 状态消息显示

```slint
// 状态消息（与标题平行）
if status-message != "": Rectangle {
    horizontal-stretch: 1;
    background: #e8f5e9;
    border-radius: 4px;
    
    Text {
        text: status-message;
        color: #2e7d32;
        font-size: 13px;
    }
}
```

### 10.2 定时清除

```slint
Timer {
    interval: 2s;
    running: status-message != "";
    triggered => {
        status-message = "";
    }
}
```

**关键经验**：
- 状态消息使用 Timer 自动清除
- 切换 Card 时手动清除状态消息
- 错误消息和成功消息使用不同颜色

---

## 十一、Git 提交规范

### 11.1 提交格式

```
<type>: <description>
```

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建、工具等

### 11.2 文档提交规则

- `plan.md`、`fixed.md` 必须单独提交
- 不要与代码文件放在同一个提交中
- 代码提交要等到用户确认后再提交

**关键经验**：
- 代码和文档分开提交
- 用户确认后再提交
- 分段提交便于回溯

---

## 十二、问题解决经验总结

### 12.1 Slint 相关

1. **全局单例必须定义在主文件中**
2. **组件的回调需要在使用处显式绑定**
3. **`vertical-stretch` 需要在父容器和子组件上都设置**
4. **`split` 和 `rsplit` 方向相反**
5. **右键使用 `pointer-event` 而不是 `clicked`**

### 12.2 Rust 相关

1. **`PathBuf` 传入函数后会被移动，需要 `clone()`**
2. **`let _ =` 忽略错误返回值**
3. **`VecModel::set_vec()` 原地更新避免模型替换崩溃**
4. **使用 `Arc` 共享数据库连接**

### 12.3 Excel 相关

1. **Excel 日期是序列号格式，需要 chrono 库转换**
2. **calamine 的 `DateTime` 类型需要 `as_f64()` 方法**
3. **孔位列可能分散在不同位置**
4. **列名需要支持多种格式**

### 12.4 文件处理相关

1. **删除文件使用 `let _ =` 忽略错误**
2. **复制文件使用 `std::fs::copy`**
3. **创建目录使用 `std::fs::create_dir_all`**

---

## 十三、项目结构

```
imgpick/
├── Cargo.toml
├── build.rs
├── src/
│   ├── main.rs              # 入口点，UI回调注册
│   ├── db/
│   │   ├── mod.rs
│   │   ├── schema.rs        # 数据库表结构
│   │   └── operations.rs    # CRUD操作
│   ├── models/
│   │   ├── mod.rs
│   │   ├── plan.rs
│   │   ├── image.rs
│   │   └── excel.rs
│   ├── ui/
│   │   ├── mod.rs
│   │   └── app.slint        # 所有UI定义
│   ├── utils/
│   │   ├── mod.rs
│   │   ├── file_utils.rs
│   │   └── excel_utils.rs
│   ├── plan_manager.rs
│   ├── image_manager.rs
│   ├── excel_manager.rs
│   └── match_engine.rs
├── plan.md
├── fixed.md
└── 学习笔记.md
```

---

## 十四、待办事项

1. **异步导入**：使用 tokio 异步运行时避免 UI 卡顿
2. **分层架构**：将业务逻辑从 main.rs 中分离
3. **数据库持久化**：保存匹配状态到数据库
4. **批量操作**：支持批量选择和匹配图片
5. **性能优化**：优化大量图片的加载和显示

---

## 十五、关键经验清单

1. Slint 组件必须显式导出才能被引用
2. 全局单例用于 Rust 和 Slint 之间的数据通信
3. 使用 `VecModel::set_vec()` 避免模型替换崩溃
4. GridLayout + for 循环在模型变空时会崩溃，需要 if 条件守卫
5. 文件删除和数据库记录删除都不会报异常
6. Excel 日期是序列号格式，需要 chrono 库转换
7. `split` 和 `rsplit` 方向相反，移除扩展名应该用 `split`
8. 独立窗口组件需要在 Rust 端创建并显示
9. 窗口关闭需要调用 `window().hide()`
10. 跨页面数据传递使用全局适配器属性作为剪贴板
11. 右键使用 `pointer-event` 而不是 `clicked`
12. 状态消息使用 Timer 自动清除
13. 子分类需要独立的选中状态跟踪
14. 表头和数据行的列宽必须一致
15. 卡片切换时需要清除选中状态和状态消息
