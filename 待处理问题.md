# 待处理问题

## 1. 窗口尺寸设置未生效

**问题描述**: 在 `app.slint` 中设置 `preferred-width: 1344px` 和 `preferred-height: 648px`，但程序启动时窗口大小未按预期显示。

**尝试过的方法**:
1. 使用固定像素值（960px, 1344px, 1440px）
2. 尝试使用百分比（70%, 60%）- Slint只支持 `100%` 作为百分比值
3. 添加 `background: white` 属性

**可能原因**:
1. Slint在Windows上的渲染行为
2. 内部布局约束可能覆盖了窗口首选大小
3. 可能需要使用Rust代码设置窗口大小

**待尝试的解决方案**:
1. 使用Rust代码在运行时设置窗口大小：
```rust
use slint::{LogicalSize, WindowSize};

let app = ui::create_app();
let window = app.window();
window.set_size(WindowSize::Logical(LogicalSize::new(1344.0, 648.0)));
app.run()?;
```

2. 检查Slint版本是否有相关bug

---

## 2. 无法调整窗口宽度（左右）

**问题描述**: 程序运行时，可以调整窗口高度（上下），但无法调整宽度（左右）。

**可能原因**:
1. Slint的Window组件默认行为
2. Windows平台的渲染后端限制
3. 内部布局的最小宽度约束

**待尝试的解决方案**:
1. 使用 `backend-winit` 特性（当前已使用）
2. 检查是否有 `min-width` / `max-width` 属性限制
3. 在Rust代码中显式设置窗口大小

---

## 3. ICU4X数据错误（Slint已知问题）

**问题描述**: 运行时持续输出 `ICU4X data error: No segmentation model for language: ja`

**相关Issue**: 
- [slint-ui/slint#11638](https://github.com/slint-ui/slint/issues/11638)
- [slint-ui/slint#11950](https://github.com/slint-ui/slint/pull/11950) - 已合并修复，等待新版本发布

**当前状态**: 此警告不影响程序功能，可安全忽略。等待Slint发布包含parley 0.10（含complex-scripts特性）的新版本。
